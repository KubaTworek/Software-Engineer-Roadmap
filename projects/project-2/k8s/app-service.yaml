apiVersion: v1
kind: Service
metadata:
  name: marketplace-app
  namespace: marketplace
spec:
  type: ClusterIP
  selector:
    app: marketplace-app
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
