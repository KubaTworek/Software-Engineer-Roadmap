server:
  port: 8080

spring:
  application:
    name: search-autocomplete-stage-6

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: search-autocomplete-stage-6
