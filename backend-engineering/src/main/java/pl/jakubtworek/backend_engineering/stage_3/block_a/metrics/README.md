# System Design Metrics Java

This package contains conceptual Java classes for observability, alert thresholds,
scaling strategy comparisons, Redis eviction policy comparisons, capacity formulas,
bottleneck prediction, cache impact modeling, and load test planning.

The code is framework-neutral and uses only the Java standard library.
All comments are written in English.
