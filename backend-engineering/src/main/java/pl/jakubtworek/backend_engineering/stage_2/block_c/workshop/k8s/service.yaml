apiVersion: v1
kind: Service
metadata:
  name: demo-api
  labels:
    app: demo-api
spec:
  type: ClusterIP

  # Service traffic goes only to Pods matching this selector and passing readiness.
  selector:
    app: demo-api

  ports:
    - name: http
      port: 80
      targetPort: http