apiVersion: v1
kind: Secret
metadata:
  name: demo-api-secret
type: Opaque
stringData:
  # This is a demo value.
  # Do not store real production secrets in plaintext Git manifests.
  DB_DSN: "postgres://app:change-me@postgres.default.svc.cluster.local:5432/app?sslmode=disable"